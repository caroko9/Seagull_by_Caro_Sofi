<!DOCTYPE html>
<html lang="en">
<%- include ('partials/head.ejs') %>

<link rel="stylesheet" href="/css/escuelaDetalle.css">

       <body>
        <%- include ('partials/header.ejs') %>
        
            <main>
                <div class="containerName">
                    <h1 class="nombreProd"> <%= escuela.nombre %></h1>                  
                </div>
          
            <div class="leftColumn">
                <div class="container">
                  <h2> ¿QUIÉNES SOMOS?</h2>
                  <p class="nombreProd"> <%= escuela.descripcion %></p>                 
                </div>
              
                <div class="container">
                  <h2>  NUESTRO EMAIL </h2>        
                  <p><%= escuela.email %> </p>       
                </div>
                <div class="container">
                  <h2>Ubicación</h2>
                  <p><%= escuela.ubicacion %></p>
                </div>
                
              </div>

       <!--boton para editar para los administradores-->

              <div class="contenedor_boton">
                <a href="/escuelas/editarEscuela/<%= escuela.id %>" class="Boton_editar">EDITAR ESCUELA</a>
              </div>


              <div class="rightColumn">
                <div class="containerImagen">
                  <img src="\img\escuelas\bordePolaroid.png" alt="Imagen polaroid">
                  <!-- se crea un if para que tome las fotos subidas en el array dentro el JSON-->
                  <% if (escuela.imagen && escuela.imagen.length > 0) { %>
                  <img class= "imagenjson" src="/img/escuelas/<%= escuela.imagen[0].filename %>" alt="Imagen de la escuela">
                  <% } %>
                </div>
              </div>    
              
       </main>
       
    </body>
</html>